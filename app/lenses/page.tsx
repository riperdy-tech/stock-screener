import { ScreenerDashboard } from "@/components/ScreenerDashboard";

// Legacy lens views (100-bagger / Reverse / Paradigm / YouTube), unchanged.
// The Factor Lab Cockpit at / is the primary decision surface.
export default function Lenses() {
    return <ScreenerDashboard />;
}
